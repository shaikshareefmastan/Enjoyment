pipeline {
   agent any
   stages {
      stage('git checkout') {
         steps {
            git 'https://github.com/shaikshareefmastan/Enjoyment.git'
         }
       }
      stage('Build application') {
         steps {
		 withDockerContainer(image: "shareefmastan/selenium:1.0"){
           script{
                               sh "pwd"
                               sh "ls -la"
                               sh "mvn clean package -e"
			
                    }
                				
            }
            
         }
      }
   }
}
