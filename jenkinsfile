pipeline {
   agent any
   stages {
      stage('git checkout') {
         steps {
            git 'https://github.com/shaikshareefmastan/Enjoyment.git'
         }
       }
      stage('Build application') {
         steps {
		 withDockerContainer(image: "csanchez/selenium-maven"){
           script{
                               sh "pwd"
                               sh "ls -la"
                               sh "mvn clean package"
			
                    }
                				
            }
            
         }
      }
   }
}
